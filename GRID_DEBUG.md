# 🔍 Диагностика проблемы с прямоугольной сеткой

## Проблема
Сетка (grid) отображается как **прямоугольник**, а не как **эллипс**, несмотря на исправления в коде.

## Возможные причины

### 1. ❌ Сервер не перезапущен после изменений
**Решение:**
```bash
# Остановите текущий сервер (Ctrl+C)
# Затем перезапустите:
python run_server.py
```

### 2. ❌ matplotlib не установлен в окружении
**Проверка:**
```bash
python check_matplotlib.py
```

**Если выдаёт ошибку:**
```bash
pip install matplotlib
# или
uv pip install matplotlib
```

### 3. ❌ Кэш фронтенда
**Решение:**
- Откройте DevTools (F12)
- Перейдите в Network
- Поставьте галочку "Disable cache"
- Обновите страницу (Ctrl+Shift+R)

### 4. ❌ Используется старая версия функции
**Проверка:**
Запустите тест:
```bash
python test_grid_shape.py
```

Ожидаемый результат:
```
✅ PASS: Grid is ELLIPTICAL (ratio < 85%)
```

Если выдаёт:
```
❌ FAIL: Grid is RECTANGULAR (ratio > 95%)
```
Значит, проблема в логике генерации.

## Пошаговая диагностика

### Шаг 1: Проверьте matplotlib
```bash
python check_matplotlib.py
```

### Шаг 2: Проверьте генерацию сетки
```bash
python test_grid_shape.py
```

### Шаг 3: Перезапустите сервер
```bash
# Остановите текущий процесс
# Затем:
python run_server.py
```

### Шаг 4: Очистите кэш браузера
- F12 → Network → Disable cache
- Ctrl+Shift+R

### Шаг 5: Запустите новую симуляцию
- Не используйте старые результаты
- Запустите новую симуляцию с фронтенда

## Проверка результата

Откройте `test_grid_output.geojson` в https://geojson.io/

**Ожидаемый результат:** Сетка должна иметь форму эллипса, а не прямоугольника.

## Если проблема сохраняется

Проверьте логи сервера на наличие строки:
```
[matplotlib.path] Found XXX cells inside polygons
```

Если вместо этого видите:
```
[FALLBACK] matplotlib not available, using custom ray-casting
```

Значит, matplotlib не установлен или не импортируется.
