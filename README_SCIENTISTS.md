# 🚀 Rocket Drop Zone Analysis - Руководство для Ученых

Это руководство предназначено для быстрого запуска инструмента анализа зон падения (OTU) на локальном компьютере.

## 📋 Предварительные требования

Перед началом работы убедитесь, что у вас установлены:

1.  **Python 3.10+**: [Скачать с официального сайта](https://www.python.org/downloads/)
    *   *Важно: При установке отметьте галочку "Add Python to PATH".*
2.  **Node.js (LTS версия)**: [Скачать с официального сайта](https://nodejs.org/)
    *   Необходим для работы интерфейса (Frontend).
3.  **Git** (опционально, но рекомендуется): [Скачать](https://git-scm.com/)

---

## 🛠️ Установка (Первый запуск)

Выполните эти действия один раз после скачивания проекта.

### 1. Настройка Backend (Серверная часть)
Откройте терминал (Command Prompt или PowerShell) в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

*Примечание: Если у вас возникли ошибки с установкой `gdal` или `geopandas`, рекомендуется использовать Anaconda или скачать готовые whl-файлы.*

### 2. Настройка Frontend (Интерфейс)
В терминале перейдите в папку `gui`:

```bash
cd gui
npm install
```

---

## ▶️ Запуск системы

Для работы системы необходимо запустить **два процесса** одновременно (в двух разных окнах терминала).

### Шаг 1: Запуск Сервера (Backend)
Запустите файл `run_server.py`. Вы можете просто дважды кликнуть по нему, если Python ассоциирован с .py файлами, или выполнить в терминале:

```bash
python run_server.py
```

*Вы увидите сообщение: `Uvicorn running on http://0.0.0.0:8000`*

### Шаг 2: Запуск Интерфейса (Frontend)
Запустите файл `run_frontend.bat` двойным кликом.

Или вручную через терминал:
```bash
run_frontend.bat
```

*Браузер должен открыться автоматически по адресу `http://localhost:5173` (или похожем).*

---

## 📊 Использование

1.  Откройте браузер по адресу, который показал Frontend (обычно **http://localhost:5173**).
2.  Используйте карту для выбора зоны интереса.
3.  Настройте параметры анализа в правой панели.
4.  Нажмите **"Calculate"** для запуска анализа.
5.  Результаты (CSV, Карты) будут сохранены в папку `outputs/exports`.

---

## ❓ Решение проблем

*   **Ошибка "Module not found"**: Повторите шаг установки зависимостей `pip install -r requirements.txt`.
*   **Ошибка "npm is not recognized"**: Установите Node.js и перезагрузите компьютер.
*   **Google Earth Engine Error**: Убедитесь, что вы авторизованы. При первом запуске система может попросить пройти аутентификацию в браузере.
